>>>markdown
# Simple Growth Calculation
How to Comupte growth as a column
>>>malloy
source: flights is table('duckdb:data/flights.parquet') + {
  measure:
    flight_count is count()
}
>>>malloy
run: flights -> {
  declare: 
    before is flight_count {where: dep_time.year = @2001}
    after is flight_count {where: dep_time.year = @2002}
  group_by: carrier
  aggregate: 
    # percent
    delta is (after-before)/before
    before
    after
}